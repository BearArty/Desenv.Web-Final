<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil do Usuário</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/estilo.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>
<body class="bg-dark text-light d-flex flex-column justify-content-between vh-100">

    <div class="container-fluid p-4 bg-primary text-white text-center">
        <h1>Gamesboxd</h1>
        <p>Sistema Crud de Cadastro de Jogos</p> 
    </div>

    <div class="container mt-4">
        <h1 class="text-center mb-4">Bem-vindo(a), <%= userName %>!</h1>
        
        <div class="text-center mb-4">
            <a href="/editar-perfil" class="btn btn-outline-light">Editar Perfil</a>
        </div>

        <br><br>
        <h2 class="mb-4 text-center">Jogos Cadastrados</h2>
        <div class="row">
            <% jogos.forEach(jogo => { %>
                <div class="col-md-4 mb-4">
                    <div class="card bg-secondary text-light h-100">
                        <% if (jogo.imagem) { %>
                            <img src="/uploads/<%= jogo.imagem %>" class="card-img-top" alt="Imagem do Jogo">
                        <% } else { %>
                            <div class="card-img-top text-center py-5">Sem imagem</div>
                        <% } %>
                        <div class="card-body">
                            <h5 class="card-title"><%= jogo.nome %></h5>
                            <p class="card-text">
                                <strong>Gênero:</strong> <%= jogo.genero_principal %><br>
                                <strong>Desenvolvedor:</strong> <%= jogo.desenvolvedor %><br>
                                <strong>Estúdio:</strong> <%= jogo.estudio %><br>
                                <strong>Data de Lançamento:</strong> <%= jogo.data_lanc %><br>
                                <strong>Descrição:</strong> <%= jogo.descricao %>
                            </p>
                        </div>
                        <div class="card-footer">
                            <% if (jogandoMap[jogo.id_jogo]) { %>
                                <a href="/editar-status/<%= jogo.id_jogo %>" class="btn btn-primary btn-block">Editar Status (Status atual: <%= jogandoMap[jogo.id_jogo] %>)</a>
                            <% } else { %>
                                <a href="/obter-jogo/<%= jogo.id_jogo %>" class="btn btn-outline-light btn-block">Obter Jogo</a>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <div class="text-center mt-4">
      <a href="/logout" class="btn btn-danger">Logout</a>
    </div>

    <footer class="text-center py-4">
        <div class="container">
            <p>&copy; 2024 Sistema Crud Node Sequelize - Arthur Bernardo. Todos os direitos reservados.</p>
            <div class="social-icons mt-3">
                <a href="#" target="_blank" class="me-2 icones"><i class="bi bi-whatsapp"></i></a> 
                <a href="https://github.com/BearArty" target="_blank" class="me-2 icones"><i class="bi bi-github"></i></a>
                <a href="https://www.threads.net/@arthur_bvp" target="_blank" class="me-2 icones"><i class="bi bi-threads"></i></a>
                <a href="https://www.instagram.com/arthur_bvp/" target="_blank" class="me-2 icones"><i class="bi bi-instagram"></i></a>
            </div>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
